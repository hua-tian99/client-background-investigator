# 问题复盘与维护注意事项

> 更新日期：2026-08-06  
> 用途：记录项目实际遇到的故障、根因、解决方式和后续维护约束。新的维护人员或 Codex 会话应与《项目交接说明》、《操作说明》一起阅读。

## 1. 快速结论

本项目的高风险区域主要是 Windows 批处理脚本、浏览器翻译对 React DOM 的干扰、Excel 行识别、飞书 DOCX 转换、飞书目录容量、功能一字段映射和功能二精准匹配。

正式批量运行前必须确认：

- 代码改动后已执行 `npm.cmd run lint` 和 `npm.cmd run build`。
- 日常未改代码时使用 `start-fast.bat`，不需要重复构建。
- 飞书文档配置的是可创建子目录的“根文件夹”，不是已满的历史文件夹。
- 功能二使用带 `view=vew...` 的多维表格链接，Excel 应从同一视图导出。
- 正式批量运行时不要额外手动点击其他订单的“立即背调”。

## 2. Windows 启动窗口构建后闪退

### 现象

`start.bat` 构建完成后 CMD 输出 `. was unexpected at this time.`，窗口随后关闭。

### 根因

CMD 会在执行前解析整个 `if (...)` 代码块。代码块中的提示文本 `Starting DEV mode (hot reload).` 含有未转义括号，被 CMD 误当成控制语法。

### 解决

- 删除或转义代码块内的括号。
- Windows 下明确调用 `npm.cmd`，避免 PowerShell `npm.ps1` 执行策略影响。
- 构建失败和服务退出后保留错误码和 `pause`。
- 新增 `start-fast.bat`，直接使用已构建的 `dist`。

### 下次注意

修改 `.bat` 时，`if` 和 `for` 代码块里的 `(`、`)`、`&`、`|` 等字符都必须按 CMD 规则处理。只测试当前分支不够，CMD 会先解析未执行的分支。

## 3. 导入 Excel 后批量背调白屏

### 现象

Chrome 控制台报 `NotFoundError: Failed to execute 'insertBefore' on 'Node'`，React 页面白屏。

### 根因

浏览器自动翻译会直接改写 React 管理的 DOM 节点。React 后续更新列表时，虚拟 DOM 与真实 DOM 不一致，导致 `insertBefore` 失败。Edge 也可能启用翻译，更换浏览器不能从根本上解决。

### 解决与注意

- 页面声明不应自动翻译，并改善 React 列表节点稳定性。
- Chrome/Edge 都应对 `localhost:3000` 关闭自动翻译。
- 白屏后先禁用翻译，再使用 `Ctrl+F5` 强制刷新。
- 浏览器插件也可能改写 DOM，排查时可使用无痕窗口对比。

## 4. Excel 订单数量比预期多

### 现象与根因

一份看似只保留约 40 条订单的 Excel，导入后显示 74 条。检查后发现，尾部仍有多行的备注、公式或其他单元格非空，解析器因此把它们当成数据行。

### 处理方法

- 在 Excel/WPS 中删除尾部多余行，不要只清空主要列。
- 检查隐藏行、备注、公式、空字符串和复制遗留内容。
- 导入后先核对订单数，数量不符时不要直接开始批量背调。

## 5. Excel 原表回写与飞书匹配被混淆

### 正确机制

- 本地 Excel 导入时保存零基 `excelRowIndex`，回写时使用 `excelRowIndex + 1` 跳过表头。
- 本地回写依赖行索引，不依赖订单号重新排序。
- 飞书功能二不使用 Excel 行号，而是使用记录 ID 和订单号定位。

### 下次注意

不要将两套机制合并，也不要尝试用 Excel 行号对应飞书行号。长订单号必须以文本存储，避免科学计数法和精度丢失。

## 6. 功能一字段写错

### 现象

“背景评判等级”没有更新，报告概要却被写入“备注”；`completedAt` 还有可能误匹配“下单时间”。

### 根因

别名匹配过宽，`grade` 没有优先命中标准结果列，通用文本兜底又把未匹配内容塞入“备注”。

### 解决

- `grade` 优先匹配“背景评判等级”，`feishuUrl` 优先匹配“背景报告文档”。
- 风险、跟进策略和跟进等级优先匹配标准同名列。
- `completedAt` 只允许匹配专用背调时间列。
- 标准表已有大量字段映射时，禁止将系统生成的 `summary` 写入“备注”。Excel 原有备注仍可同名写入。

### 下次注意

新增别名时应先评估是否会命中其他业务列。“时间”、“等级”、`id` 这类通用词不应单独作为别名。

## 7. 功能二使用公司名称匹配不可靠

### 原问题

公司名称可能存在大小写、缩写、人名、空值或重复，不足以安全定位已有订单。早期宽泛 `id` 规则还存在把商品 ID 误当订单号的风险。

### 当前规则

1. 同时存在记录 ID 和订单号时，先用顶层 `record_id` 精确定位，再对订单号做字符串精确校验。
2. 记录 ID 不存在或订单号冲突时跳过，不能降级匹配。
3. 缺少记录 ID 时，只有订单号唯一命中才允许兜底。
4. 公司名称、商品 ID、EANcode 和商品编码都不参与定位。
5. 只更新背景评判等级、背景报告文档、跟进策略、跟进等级和风险提示五列。

## 8. 功能二遍历整表慢、读取 2500 条后匹配失败

### 问题与解决

- 旧实现最多读取 5 页、每页 500 条，后部记录根本没有进入匹配集合。
- 改为持续分页到 `has_more=false`，缺少或重复 `page_token`、中间页失败时整次停止，禁止用不完整数据继续匹配。
- 配置链接中的 `view=vew...` 现在会传给飞书 API，只读取指定视图，速度和范围都与视图导出 Excel 一致。
- 终端会输出 `record_id_not_found`、`record_id_order_conflict`、`duplicate_order_number` 等具体原因和有限诊断样例。

### 下次注意

不要恢复固定页数上限。如果用户明确使用某个视图，必须保留 `view_id`；否则性能问题和范围错误会同时复发。

## 9. 飞书链接少量是 `/file/` 而不是 `/docx/`

### 根因

飞书 import task 的 `job_status=2` 只表示任务成功，不代表最终产物一定是在线 DOCX。部分任务会“成功”但返回 `file`。

### 解决

- 同时检查 `job_status`、`result.file_type/result.type` 和 URL 路径，URL 为最终判定。
- 产物为 `file` 或 `job_status=3` 时最多尝试 3 次，每次重新上传 DOCX 获取新 `file_token`。
- 轮询查询偶发失败时继续使用同一 ticket；超时后再做一次终态确认。
- 最终仍失败时允许降级 `/file/`，并返回 `importFallback`、`importAttempts` 和 `importError`。

### 下次注意

不能只看 `job_status`。状态不明或仍在处理时不要新建 import task，否则旧任务稍后成功会产生重复文档。重试会留下原始云空间文件，当前出于数据安全不自动删除。

## 10. 飞书目录达到单层节点上限

### 现象

运行数百条后出现 `1062507 / parent node out of sibling num`。云文档创建失败，而旧逻辑仍用空 `feishuUrl` 同步多维表，导致其他四列正常、文档链接为空。

### 根因

飞书单个文件夹/根层的子节点数有上限。源 DOCX、最终在线文档和重试留下的文件都会消耗节点。

### 当前解决方式

- 用户配置一个能够创建子目录的空根文件夹。
- 程序首次上传时创建 `背调报告-YYYYMMDD-001`。
- 当子目录返回 `1062507` 时，在根目录下创建 `002`，重新上传刚才失败的订单，成功后再继续。
- 根目录级使用串行锁，防止并发请求重复创建新目录。
- 子目录 Token 保存到浏览器 `localStorage`，重启后继续使用。
- 云文档失败时不再同步多维表五列；手动重试文档成功后再自动补做多维表同步。

### 重要边界

已经达到上限的文件夹不能直接作为“根文件夹”，因为新建子文件夹本身也是新节点，会立即再次返回 `1062507`。必须提供未满且有创建权限的根目录。

## 11. 手动删除子文件夹后出现 `parent node not exist`

### 现象与根因

日志先显示 `[Feishu Folder] Restored subfolder index=1 from client state.`，随后上传报 `parent node not exist`。这表示浏览器仍缓存旧子目录 Token，但用户已经在飞书中手动删除该目录。

### 当前处理

1. 打开飞书同步配置。
2. 清空“报告文档根文件夹 Token”。
3. 重新粘贴有效的根文件夹链接或 Token。
4. 重试失败订单，程序会重新创建 `001`。

### 后续需注意

- 批量运行期间不要手动删除、移动或重组程序正在使用的子文件夹。
- 当前自动恢复主要覆盖“目录已满”；对“目录被人工删除”仍应优先重置配置和缓存。

## 12. 第一个订单看起来很慢或“卡在创建文档”

### 原因

第一条需要顺序完成 DeepSeek 联网背调、获取飞书凭证、创建首个子目录、转换 DOCX、上传、创建 import task 和轮询。DeepSeek 单次请求允许最长约 120 秒，飞书转换也需要轮询。后续订单可复用已创建的子目录，通常更快。

如果终端只有：

```text
[AI Investigator] Attempt 1 of 3 (Web Search: true)...
```

则说明仍在 AI 阶段，尚未进入 DOCX/飞书上传。后续正常日志应陆续出现：

```text
[Feishu Folder] Created/Restored ...
Successfully converted HTML report to binary .docx file ...
[Feishu Import] ...
[Bitable Sync] ...
```

界面可能暂时保留上一次的“创建文档中”状态，因此排查时应以终端当前最后一条阶段日志为准。超过约 3–5 分钟仍没有任何新日志才应视为异常。

## 13. 批量运行时手动发起背调

批量背调默认逐条执行。如果同时手动点击其他订单的“立即背调”，会产生额外并发请求。它通常不会改变 Excel 行定位，但会增加：

- DeepSeek API 限频和 Token 消耗。
- 飞书文档同时上传和目录切换压力。
- 同一记录被重复写入或重复创建文档的风险。

正式历史订单批量任务中应避免这样操作。

## 14. 报告标题和参考网站结构

### 已调整规则

- 飞书在线文档标题为：`收件人名称-中文国家名称-中文产品名称`。
- 收件人以 Excel 原始“收件人名称”为准，缺失时才使用公司/客户名称。
- AI 返回 `countryNameZh` 和 `productNameZh`，缺失时使用本地国家、产品关键词映射兜底。
- “参考网站”必须位于报告最后，固定为 `## 五、参考网站 (References)`。
- 服务端会规范化 AI 输出的参考区块，使 Markdown、Word、ZIP 和飞书 DOCX 的顺序一致。

### 下次注意

改报告提示词时必须同时检查服务端结构规范化，不能只改 AI 模板。调整标题时不应改变本地导出文件名、Excel 回写或功能二匹配标识。

## 15. Windows 便携版

### 解决的问题

大批量历史订单需要多台独立设备处理，但目标设备没有 Node.js、npm 和开发依赖。项目采用“ZIP + 内置 Windows x64 Node.js Runtime”，不使用 Electron 或单文件 EXE。

### 构建和分发

```powershell
npm.cmd run package:portable
```

产物位于 `release` 目录，包含 ZIP 和 SHA-256 校验文件。每次源码修改后都必须重新打包；普通开发版构建不会自动更新已分发的 ZIP。

便携包不包含 DeepSeek Key、飞书 App Secret、表格链接和客户数据。每台机器必须在浏览器中独立配置。

## 16. 标准排错次序

遇到异常时不要只看前端文案，应按下列顺序定位：

1. 记录是功能一还是功能二，使用哪个 Excel、表格和视图。
2. 查看服务终端最后一条日志，确认卡在 AI、DOCX、飞书 import 还是 Bitable。
3. 查看浏览器 Network 中 `/api/investigate`、`/api/feishu/upload` 和 `/api/feishu/sync-bitable` 的状态码与响应。
4. 白屏时查看 Console；字段错位时查看 Mapping Analysis；功能二失败时查看 `matchReasonCounts` 和 `matchDiagnostics`。
5. 只用 1–3 条数据做最小化复现，确认后再运行大批次。
6. 修改代码后执行：

```powershell
npm.cmd run lint
npm.cmd run build
```

7. 若需分发便携版，再执行 `npm.cmd run package:portable`，并核对 SHA-256。

## 17. 正式批量前检查清单

- [ ] Excel 订单数与界面导入数一致。
- [ ] 订单号未变成科学计数法，记录 ID 为当前目标表的 `rec...`。
- [ ] 功能二的 Excel 和飞书链接来自同一视图。
- [ ] 飞书 App 具有目录创建、文件上传、文档导入和多维表读写权限。
- [ ] 配置的文档目录是可写的未满根目录。
- [ ] 未手动删除程序正在使用的报告子目录。
- [ ] Chrome/Edge 已对本地页面关闭自动翻译。
- [ ] 先用 1–3 条验证报告、DOCX、五个结果列和匹配诊断。
- [ ] 备份目标多维表格或至少保留可回滚的视图导出文件。

## 18. 不能轻易改回的约束

- 功能二不能恢复公司名称兜底。
- 订单号别名不能包含通用 `id`。
- 功能二有记录 ID 时必须与订单号双重校验。
- 功能一不能把 `summary` 自动写入标准表“备注”。
- `completedAt` 不能匹配下单时间、付款时间或通用“时间”。
- 分页读取不能在 `has_more=true` 时因固定页数上限停止。
- 飞书 DOCX 成功不能只根据 `job_status=2` 判定。
- 云文档链接未生成时，不能把空链接与其他四列当作完整结果同步。
- 便携包不能打入 API Key、App Secret、客户 Excel 或本地浏览器配置。

