# 项目交接说明

> 已知问题、历史根因和维护约束请同时阅读：[问题复盘与维护注意事项.md](./问题复盘与维护注意事项.md)

> 更新日期：2026-08-03  
> 项目目录：`E:\projects\client-background-investigator-v1.0`  
> 用途：供新的 Codex 会话或维护人员快速理解当前项目状态。开始新任务前，请先阅读本文，再按需查看《操作说明》和源码。

## 1. 新会话建议开场语

可在新的 Codex 窗口直接发送：

> 请先完整阅读项目根目录的 `项目交接说明.md` 和 `操作说明.md`，再检查当前源码。以现有代码为准，不要恢复已经移除的旧匹配逻辑；修改后运行 `npm.cmd run lint` 和 `npm.cmd run build`。

若任务涉及具体故障，请在这段话后附上终端日志、浏览器控制台截图、使用的功能（功能一或功能二）以及测试 Excel。

## 2. 项目用途与技术结构

本项目用于导入境外订单或客户资料，调用 DeepSeek 联网背调，生成客户评级、报告概要、风险提示、跟进策略和跟进等级，并可导出本地文件或同步到飞书。

主要技术：

- 前端：React 19、TypeScript、Vite，主要代码集中在 `src/App.tsx`。
- 后端：Express、TypeScript，接口和大部分业务逻辑集中在 `server.ts`。
- Excel：`xlsx`。
- Word：`html-to-docx`。
- 飞书：云空间文件上传、DOCX 在线文档导入、多维表格读写。
- 生产产物：前端与后端均输出到 `dist`，后端入口是 `dist/server.cjs`。

项目目前没有拆分成大量模块。排查界面、Excel 导入和前端状态时先看 `src/App.tsx`；排查 AI、飞书文档、多维表格字段映射时先看 `server.ts`。

## 3. 启动与构建

### 日常使用（代码没有改变）

双击：

```text
start-fast.bat
```

它会检查 `dist/server.cjs` 和 `dist/index.html`，随后复用 `start.bat fast`，不会重新构建。

### 修改代码后

双击：

```text
start.bat restart
```

或在项目目录运行：

```powershell
npm.cmd run lint
npm.cmd run build
npm.cmd start
```

常用脚本：

| 命令 | 作用 |
|---|---|
| `start.bat` | 构建并启动生产服务 |
| `start.bat fast` | 不构建，启动现有生产产物 |
| `start.bat dev` | 开发热更新模式 |
| `start.bat restart` | 停止 3000 端口旧进程、重新构建并启动 |
| `start-fast.bat` | 日常双击快速启动入口 |
| `npm.cmd run lint` | TypeScript 类型检查 |
| `npm.cmd run build` | 构建前端和 `dist/server.cjs` |

Windows 下应明确使用 `npm.cmd`，避免 PowerShell 的 `npm.ps1` 执行策略问题。

### Windows 免环境便携版

- 构建命令：`npm.cmd run package:portable`。
- 构建脚本：`scripts/build-portable.ps1`；模板位于 `portable`目录。
- 输出：`release/Client-Background-Investigator-Portable-win-x64-<版本>.zip`及 SHA-256 文件。
- 包内后端会将运行依赖打入单个 `server.cjs`，并携带开发机当前 Windows x64 `node.exe`和对应 Node.js许可证，不包含源码、npm、`node_modules`或开发依赖。
- 本机 Node 安装缺少 LICENSE 时，打包脚本从 Node.js 官方源码仓库按精确版本下载；下载失败会停止打包。
- 便携启动器设置 `NODE_ENV=production`并从 `app`目录运行，因此静态文件仍从 `app/dist`加载。
- `server.ts`中的 Vite 使用动态导入，仅开发模式加载，不能恢复为顶层静态导入，否则便携包会捆入 Vite及其原生开发依赖。
- ZIP 不包含 DeepSeek/飞书凭据。每台目标电脑在浏览器中独立配置，适合独立 Excel、独立飞书表的多设备历史订单处理。

## 4. 核心业务流程

```mermaid
flowchart LR
    A["Excel / CSV / 粘贴 / 手工录入"] --> B["React 线索 Lead"]
    B --> C["POST /api/investigate"]
    C --> D["DeepSeek 联网背调"]
    D --> E["评级、概要、风险、策略、跟进等级"]
    E --> F["本地 Excel / Word / Markdown / ZIP"]
    E --> G["POST /api/feishu/upload"]
    G --> H["飞书 DOCX 或降级 file URL"]
    E --> I["POST /api/feishu/sync-bitable"]
    H --> I
    I --> J["功能一新增记录 / 功能二精准更新"]
```

批量背调默认逐条执行。批量任务运行时再手动点击某一条“立即背调”，会形成额外并发请求；通常不会破坏数据，但会增加 API 限频、Token 消耗、飞书上传并发及同一记录重复写入风险，测试和正式批量运行时应避免这样操作。

## 5. Excel 导入与原表回写

- 导入时每条线索保存零基 `excelRowIndex`。
- 写回时使用 `excelRowIndex + 1` 定位工作表数组中的数据行，以跳过表头。
- 本地回写依靠行索引，不依靠订单号重新排序。
- 原表缺少结果列时追加结果列；已有标准列时复用。
- 长订单号应按字符串保留，不能转为科学计数法或有损数字。

曾出现“Excel 约 40 条却导入 74 条”的情况。已确认测试文件中实际有 38 条订单行，另有 36 行只有“备注”列非空，因此解析器将它们视为有效数据。若再次出现数量异常，应先检查隐藏行、尾部格式和仅有备注/公式的行，不要直接认定是背调或飞书代码造成的。

## 6. 飞书功能一：新建客商入库

功能一使用 `batch_create` 新增多维表格记录，会映射原始 Excel 字段和背调结果字段。

标准背调结果列：

| 程序字段 | 飞书标准列 |
|---|---|
| `grade` | `背景评判等级` |
| `feishuUrl` | `背景报告文档` |
| `riskAlert` | `风险提示` |
| `followUpStrategy` | `跟进策略` |
| `followUpGrade` | `跟进等级` |

2026-08-03 已修复的功能一问题：

- `grade` 现在优先匹配“背景评判等级”。
- `feishuUrl` 现在优先匹配“背景报告文档”。
- `completedAt` 只匹配“背调时间、背调完成时间、调查时间、调查完成时间”等专用列，不再覆盖“下单时间、付款时间”。
- 标准表已有大量字段成功映射时，系统不会再把评级和报告概要拼接写入“备注”。
- Excel 原有非空“备注”仍通过同名精确匹配写入。
- 标准表没有“报告概要”列时，`summary` 跳过，不借用“备注”。
- 通用文本兜底仅用于该记录完全没有任何字段成功映射，或目标表只有一个文本字段的场景。

功能一相关修改主要位于 `server.ts` 的 `keyAliases`、字段映射循环和 Smart Fallback 区块。

## 7. 飞书功能二：精准匹配更新

功能二只更新五个背调结果列，不更新订单、金额、物流、客户资料和备注。

匹配规则必须保持如下行为：

1. 输入同时有“记录 ID”和“订单号”时，先用输入记录 ID 精确匹配飞书 API 返回的顶层 `record_id`。
2. 找到记录后，再精确比较该飞书记录的订单号。
3. 两者一致才更新。
4. 有记录 ID 但记录不存在或订单号冲突时，跳过，不能降级为订单号匹配。
5. 输入缺少记录 ID但有订单号时，仅当订单号在飞书中唯一命中一条记录才允许兜底更新。
6. 重复订单号、订单号未找到、标识缺失均跳过并返回诊断。

注意事项：

- 公司名称不参与功能二匹配，不能恢复公司名称兜底。
- 通用 `id` 不能作为订单号别名。
- `商品ID`、`EANcode`、`商品编码`绝不能参与订单记录定位。
- “记录 ID”通常是 `rec...`，应对应当前目标飞书多维表格的内部记录 ID。
- 订单号按去除首尾空白后的字符串精确比较。
- 每批最多 100 条 `batch_update`。
- 配置链接包含 `view=vew...` 时，后端会将其作为 `view_id`传给飞书，只分页读取该视图中的记录；这应与从该视图导出的 Excel 数据范围保持一致。
- 配置链接没有视图参数时仍兼容读取整张表，但终端会输出警告。视图筛选掉的记录不会参与功能二匹配。

接口统计包括 `recordIdMatchedCount`、`orderFallbackMatchedCount`、`conflictCount`、`unmatchedCount` 以及有限诊断样例。

## 8. 飞书 DOCX 导入与 `/file` 降级

报告 Markdown 在接口返回前通过 `src/reportFormatting.ts`规范化：“参考网站/参考来源/References”会从原位置移除并统一追加为最后的 `## 五、参考网站 (References)`。若正文没有参考区块则使用联网搜索 `sources`，两者都没有时写明未检索到公开来源。修改报告结构时必须回归订单收货信息不会被一并移除。

`POST /api/feishu/upload` 会先上传二进制 DOCX，再创建飞书导入任务。

在线文档标题规则为 `收件人名称-中文国家名称-中文产品名称`。Excel 导入必须单独保留 `recipientName`；AI 响应提供可选的 `countryNameZh`和 `productNameZh`，前端用本地国家/产品规则兜底后生成 `documentTitle`。上传接口优先使用该可选字段，旧请求仍按 `companyName`生成旧标题。本地导出文件名不随之改变。

当前规则：

- 只有最终 URL 路径为 `/docx/` 才算在线文档成功。
- `job_status = 2` 表示任务成功，但若结果仍为 `file` 或 URL 是 `/file/`，属于静默转换回退，需要重试。
- `job_status = 3` 是明确失败，可以重试。
- 总计最多 3 次；第 2、3 次前等待 2 秒、5 秒。
- 每次尝试重新上传 DOCX，获得新的 `file_token`。
- 同一 ticket 每 2 秒轮询一次，最多 30 次；查询偶发失败继续轮询同一 ticket。
- 轮询超时后等待 5 秒做最终确认。若仍处于处理中，不创建新任务，直接降级，避免稍后产生重复 DOCX。
- 权限、文件夹、参数等不可恢复错误直接降级。
- 三次仍失败时返回 `/file`，业务流程继续，并写入多维表格。

响应元数据：

- `urlType: "docx" | "file"`
- `importAttempts`
- `importFallback`
- `importError`
- `importJobStatus`
- `importFileType`

前端对降级结果显示“飞书原始文件”。此前 100 条测试约 9 条为 `/file`，增加重试机制就是为了降低该比例。后续测试应统计首次成功、重试成功和最终降级数量，同时检查没有生成重复在线文档。

## 9. 主要接口

| 接口 | 作用 |
|---|---|
| `POST /api/investigate` | 调用 DeepSeek 完成单条背调 |
| `POST /api/feishu/upload` | 上传 DOCX并导入飞书在线文档 |
| `POST /api/feishu/upload-sheet` | 上传表格相关文件 |
| `POST /api/feishu/sync-bitable` | 功能一新增或功能二更新多维表格 |

修改接口时应保持现有请求格式兼容，尤其不要破坏前端已经发送的原始 Excel 字段。

## 10. 浏览器与前端注意事项

- 曾出现 Chrome 批量背调后白屏，控制台报 React `insertBefore` DOM 异常。
- 根因与浏览器自动翻译修改 React 管理的 DOM 有关，不是 Edge 天然不会发生。
- 当前页面已设置中文语言和禁止翻译，并调整了加载提示 DOM 的稳定性。
- Chrome 和 Edge 均可使用，但应关闭该页面的自动翻译；出现异常后使用 `Ctrl+F5` 强制刷新。
- 背调结果主要保存在当前 React 会话中，刷新页面可能丢失尚未导出的结果。
- DeepSeek 与飞书凭据保存在浏览器 `localStorage`，不是加密存储，不要在公共电脑长期保存。

## 11. 配置和安全

- DeepSeek Key 可由界面配置；`.env.example` 仅是示例，不得把真实 Key 写入文档或提交源码。
- 飞书 App ID、App Secret、Folder Token、Wiki/Base URL需与目标租户和多维表格对应。
- 日志不得输出 Access Token 或 App Secret。
- 功能二同步前建议备份多维表格，并先用少量数据验证。
- 订单号和记录 ID应使用稳定的文本表现并保持唯一。

## 12. 已验证状态

截至 2026-08-03 最近一次修改后：

- `npm.cmd run lint`：通过。
- `npm.cmd run build`：通过。
- `dist/server.cjs` 和前端生产文件已经更新。
- Vite 有约 980 KB 主包大于 500 KB 的警告，但不是构建失败，也不是当前功能故障。

当前没有自动化单元测试。业务回归主要依靠 TypeScript 检查、生产构建、小批量 Excel 和测试飞书表验证。

## 13. 测试资料

用户曾提供以下本地测试文件（路径可能在未来被移动或删除，且包含客户数据，禁止复制内容到文档或外部服务）：

- 标准未背调表：`C:\Users\Example\Desktop\订单信息管理_示例订单表_未背调.xlsx`
- 背调成功表：`C:\Users\Example\Desktop\订单信息管理_示例订单表_背调成功客户.xlsx`
- 重试测试表：`C:\Users\Example\Desktop\重试test订单信息管理_示例订单表_未背调.xlsx`

标准表约 59 列，关键列包括“订单号、记录 ID、背景评判等级、背景报告文档、跟进策略、跟进等级、风险提示”。

## 14. 修改后的最低验收清单

每次改变业务代码后至少完成：

1. 运行 `npm.cmd run lint`。
2. 运行 `npm.cmd run build`。
3. 若服务正在运行，用 `start.bat restart` 加载新构建。
4. 验证 Excel 导入条数，注意只有备注或公式的尾部行。
5. 验证一条单独背调与一个小批量背调。
6. 若改功能一：检查五个标准结果列、备注和订单时间。
7. 若改功能二：检查双标识一致、冲突、无效记录 ID、唯一订单号兜底和重复订单号。
8. 若改飞书文档：检查 `/docx`、重试成功、最终 `/file` 降级和重复文档。
9. 查看浏览器控制台和服务终端是否出现新错误。

## 15. 维护原则

- 先复现和读日志，再改代码；用户正在跑批量测试时不要重启服务或修改构建产物。
- 诊断请求不等于授权修改，用户明确要求修复后再实现。
- 保持功能一和功能二分支边界清晰，修复一方时做另一方回归。
- 不要用公司名称恢复功能二匹配。
- 不要让宽泛别名（如 `id`、`时间`、`日期`、`等级`）抢先命中错误业务列。
- 本地 Excel 行索引回写与飞书业务标识匹配是两套机制，不要混用。
- 不要自动删除飞书重试留下的源文件，除非用户明确授权并确认精确目标。
- 现有工作区可能没有可用 Git 历史，修改前后要通过源码检查和构建结果确认状态。

## 16. 相关文档和源码入口

- `操作说明.md`：面向业务人员和维护人员的完整使用与原理说明。
- `README.md`：项目概览、接口和开发说明。
- `src/App.tsx`：前端界面、Excel 导入、状态和用户操作。
- `server.ts`：DeepSeek、飞书文档、多维表格同步和字段映射。
- `start.bat`：完整构建/开发/重启入口。
- `start-fast.bat`：免构建快速启动入口。
